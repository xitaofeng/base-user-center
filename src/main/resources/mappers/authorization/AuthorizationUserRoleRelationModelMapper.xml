<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shsnc.base.authorization.mapper.AuthorizationUserRoleRelationModelMapper">

    <!-- 表名用变量表示 -->
    <sql id="tableName">authorization_user_role_relation</sql>

    <sql id="Base_Column">
        id,
        user_id,
        role_id
    </sql>

    <insert id="batchAddAuthorizationUserRoleRelationModel">
        INSERT INTO
        <include refid="tableName"/>
        (
        user_id,
        role_id
        )
        VALUES
        <foreach collection="authorizationGroupRoleRelationModels" item="item" index="index" separator="," >
            (#{item.userId},#{item.roleId})
        </foreach>
    </insert>

    <select id="getRoleIdByGroupId" resultType="java.lang.Integer">
        SELECT
        role_id
        FROM
        <include refid="tableName"/>
        WHERE user_id = #{userId }
    </select>

    <delete id="deleteAuthorizationUserRoleRelationModelByUserId">
        DELETE FROM
        <include refid="tableName"/>
        WHERE user_id = #{userId }
    </delete>

</mapper>